{
  "name": "kindred-studio",
  "version": "1.0.0",
  "private": true,
  "description": "A therian mask and character creator. The app itself ships with no dependencies; these are test tooling only.",
  "type": "module",
  "scripts": {
    "serve": "node tests/server.mjs",
    "test:unit": "node --test tests/smoke.test.mjs",
    "test:e2e": "playwright test",
    "screenshots": "SHOT_DIR=screenshots playwright test screenshots.spec.mjs",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  },
  "engines": {
    "node": ">=20"
  }
}
